import Card from "components/Card";
import * as s from "./styles";

const Login: React.FC = () => {
  return (
    <s.Login>
      <s.Logo />
      <Card />
    </s.Login>
  );
};

export default Login;
